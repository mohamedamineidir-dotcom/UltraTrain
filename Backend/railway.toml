[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./App serve --env production --hostname 0.0.0.0 --port 8080"
healthcheckPath = "/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"

[environments.production.variables]
LOG_LEVEL = "notice"
